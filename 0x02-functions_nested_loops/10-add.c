#include "main.h"
/**
* add - Entry point * Description: add function is used in a program
* that adds two itegers and returns the result.
* @a: the first integer
* @b: the second integer
* Return: a + b
*/

int add(int a, int b)
{
	return (a + b);
}
